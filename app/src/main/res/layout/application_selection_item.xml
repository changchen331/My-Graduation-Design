<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/application_selection_item"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@drawable/application_selection_item"
    android:orientation="vertical"
    android:paddingStart="5dp"
    android:paddingTop="3dp"
    android:paddingEnd="5dp"
    android:paddingBottom="3dp">

    <!-- APP 图标 -->
    <ImageView
        android:id="@+id/application_selection_item_image"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:importantForAccessibility="no"
        android:scaleType="fitCenter"
        android:src="@drawable/application_selection_item_default" />

    <!-- APP 名称 -->
    <TextView
        android:id="@+id/application_selection_item_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:autofillHints="text"
        android:gravity="center"
        android:text="@string/application_selection_item_name"
        android:textColor="@color/application_selection_item_text"
        android:textSize="16sp" />

</LinearLayout>
